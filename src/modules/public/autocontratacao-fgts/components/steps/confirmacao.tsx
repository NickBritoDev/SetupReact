import { Text } from "@chakra-ui/react";
import { IStepProps } from "../../types/steps";

export default function ConfirmacaoComponent(props: IStepProps) {
  return <Text>Confirmação de Proposta</Text>;
}
