import { Text } from "@chakra-ui/react";
import { IStepProps } from "../../types/steps";

export default function CadastroDadosPessoaisComponent(props: IStepProps) {
  return <Text>Cadastro de Dados Pessoais</Text>;
}
